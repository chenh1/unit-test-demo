// Testing arrays
var expect = require('chai').expect;


function testArray()  {
  // return [];
}

/*
function testConditions(value) {
  // return one of three unique strings based on what 'value' is
  if (value === 0) {
    // return '';
  }
  else if (value === 1) {
    // return '';
  }
  else if (value === 2) {
    // return '';
  }
}
*/

/*
function testConcatentation(string) {
  // concatenate 'string' with another string and write a test for it
  // return '';
}
*/

describe('arrays.js', function() {
  describe('testString', function() {
    it('should return a string', function() {
      // expect(testString()).to.equal();
    });
  });

  /*
  describe('testConditions', function() {
    it('should return a specific string', function() {
      // expect(testConditions(0)).to.equal('');
      // expect(testConditions(1)).to.equal('');
      // expect(testConditions(2)).to.equal('');
    });
  });
  */

  /*
  describe('testConcatentation', function() {
    it('should concatenate a string', function() {
      // expect(testConcatentation('')).to.equal('');
    });
  });
  */
});
