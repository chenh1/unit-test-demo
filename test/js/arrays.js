// Testing arrays
var expect = require('chai').expect;


function testArray()  {
  // create an array of strings
  // return [];
}


describe('arrays.js', function() {
  describe('testArray', function() {
    it('should return an array', function() {
      // var array = testArray();
      // expect(array.length).to.equal();
      // expect(array[0].length).to.equal();
      // expect(array[1].length).to.equal();
      // etc
    });
  });
});
